# `<>` で囲われた要素に適切な値を指定
# 例 : `<IP>` -> `127.0.0.1`

# <Server Windows IP>: WSL を動作させる Windows の IP
Host <Server Windows IP>
  HostName <Server Windows IP>
  # <Server WSL User>: WSL で使用するユーザー名
  User <Server WSL User>
  # <Server WSL Port>: WSL を動作させる Windows の接続可能としたポート (指定なしでデフォルトでも問題ないかもしれない)
  Port <Server WSL Port>
